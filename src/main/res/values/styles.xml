<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="BtnConnect" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/btnGreen</item>
    </style>
    <style name="BtnDisconnect" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/btnRed</item>
    </style>
    <style name="BtnOpen" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/btnBlue</item>
    </style>
    <style name="BtnClose" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/btnOrange</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
